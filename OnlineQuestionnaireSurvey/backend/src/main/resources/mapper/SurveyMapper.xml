<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.survey.mapper.SurveyMapper">

    <resultMap id="BaseResultMap" type="com.survey.entity.Survey">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="cover_image" property="coverImage"/>
        <result column="user_id" property="userId"/>
        <result column="status" property="status"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="is_anonymous" property="isAnonymous"/>
        <result column="max_responses" property="maxResponses"/>
        <result column="response_count" property="responseCount"/>
        <result column="view_count" property="viewCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectSurveyPage" resultMap="BaseResultMap">
        SELECT s.*, u.nickname
        FROM survey s
        LEFT JOIN sys_user u ON s.user_id = u.id
        WHERE s.deleted = 0
        <if test="userId != null">
            AND s.user_id = #{userId}
        </if>
        <if test="status != null">
            AND s.status = #{status}
        </if>
        ORDER BY s.create_time DESC
    </select>

</mapper>