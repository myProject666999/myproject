<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.micro.frontend.mapper.GrayUserMapper">

    <resultMap id="BaseResultMap" type="com.micro.frontend.entity.GrayUser">
        <id column="id" property="id"/>
        <result column="gray_release_id" property="grayReleaseId"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="user_type" property="userType"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="selectByGrayReleaseId" resultMap="BaseResultMap">
        SELECT * FROM gray_user WHERE gray_release_id = #{grayReleaseId} ORDER BY created_at
    </select>

    <select id="selectByGrayReleaseIdAndUserId" resultMap="BaseResultMap">
        SELECT * FROM gray_user WHERE gray_release_id = #{grayReleaseId} AND user_id = #{userId} LIMIT 1
    </select>

    <insert id="insert" parameterType="com.micro.frontend.entity.GrayUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO gray_user (gray_release_id, user_id, user_name, user_type, created_at)
        VALUES (#{grayReleaseId}, #{userId}, #{userName}, #{userType}, #{createdAt})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO gray_user (gray_release_id, user_id, user_name, user_type, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.grayReleaseId}, #{item.userId}, #{item.userName}, #{item.userType}, #{item.createdAt})
        </foreach>
    </insert>

    <delete id="deleteByGrayReleaseId">
        DELETE FROM gray_user WHERE gray_release_id = #{grayReleaseId}
    </delete>

    <delete id="deleteByGrayReleaseIdAndUserId">
        DELETE FROM gray_user WHERE gray_release_id = #{grayReleaseId} AND user_id = #{userId}
    </delete>
</mapper>
