<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.micro.frontend.mapper.MicroAppMapper">

    <resultMap id="BaseResultMap" type="com.micro.frontend.entity.MicroApp">
        <id column="id" property="id"/>
        <result column="app_code" property="appCode"/>
        <result column="app_name" property="appName"/>
        <result column="description" property="description"/>
        <result column="current_version" property="currentVersion"/>
        <result column="status" property="status"/>
        <result column="owner" property="owner"/>
        <result column="owner_email" property="ownerEmail"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByAppCode" resultMap="BaseResultMap">
        SELECT * FROM micro_app WHERE app_code = #{appCode} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM micro_app
        <where>
            deleted = 0
            <if test="status != null">AND status = #{status}</if>
            <if test="keyword != null and keyword != ''">AND (app_code LIKE CONCAT('%', #{keyword}, '%') OR app_name LIKE CONCAT('%', #{keyword}, '%'))</if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM micro_app
        <where>
            deleted = 0
            <if test="status != null">AND status = #{status}</if>
            <if test="keyword != null and keyword != ''">AND (app_code LIKE CONCAT('%', #{keyword}, '%') OR app_name LIKE CONCAT('%', #{keyword}, '%'))</if>
        </where>
    </select>

    <select id="selectAllActive" resultMap="BaseResultMap">
        SELECT * FROM micro_app WHERE status = 1 AND deleted = 0 ORDER BY app_code
    </select>

    <insert id="insert" parameterType="com.micro.frontend.entity.MicroApp" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO micro_app (
            app_code, app_name, description, current_version, status,
            owner, owner_email, created_at, updated_at, deleted
        ) VALUES (
            #{appCode}, #{appName}, #{description}, #{currentVersion}, #{status},
            #{owner}, #{ownerEmail}, #{createdAt}, #{updatedAt}, #{deleted}
        )
    </insert>

    <update id="update" parameterType="com.micro.frontend.entity.MicroApp">
        UPDATE micro_app
        <set>
            <if test="appCode != null">app_code = #{appCode},</if>
            <if test="appName != null">app_name = #{appName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="currentVersion != null">current_version = #{currentVersion},</if>
            <if test="status != null">status = #{status},</if>
            <if test="owner != null">owner = #{owner},</if>
            <if test="ownerEmail != null">owner_email = #{ownerEmail},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt}</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE micro_app SET status = #{status}, updated_at = NOW() WHERE id = #{id}
    </update>

    <update id="updateCurrentVersion">
        UPDATE micro_app SET current_version = #{version}, updated_at = NOW() WHERE id = #{id}
    </update>

    <delete id="deleteById">
        UPDATE micro_app SET deleted = 1, updated_at = NOW() WHERE id = #{id}
    </delete>
</mapper>
