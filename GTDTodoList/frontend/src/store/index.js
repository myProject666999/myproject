import { createStore } from 'vuex'

export default createStore({
    state: {
        userId: 1,
        apiBaseUrl: 'http://localhost:8080/api'
    },
    mutations: {},
    actions: {},
    modules: {}
})
